const errorMessage = {
  INVALID_RETURN_STATEMENT: "Encountered return statement outside of function",
  INVALID_LEFT_HAND_SIDE: "Invalid left hand side of assignment operator",
};

export default errorMessage;
